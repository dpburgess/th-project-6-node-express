extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      h1 Portfolio of Projects

      p.lead.text-light
        | This project will be used to display previously completed projects as I learn about pug templates, node, npm, and the Express framework.
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      each project in body
        .cell
          a(href=`/project/${project.id}`)
          img.thumbnail(src=`../../${project.image_urls[0]}`)
          h5 #{project.project_name}
      
